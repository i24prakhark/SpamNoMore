# SpamNoMore API Configuration

# CORS Configuration
# Comma-separated list of allowed origins (default: "*" for all)
# Example: CORS_ORIGINS=https://example.com,https://app.example.com
# CORS_ORIGINS=*

# DNS Configuration
# Timeout for DNS queries in seconds (default: 5)
# DNS_TIMEOUT=5

# DNS lifetime for queries in seconds (default: 5)
# DNS_LIFETIME=5
